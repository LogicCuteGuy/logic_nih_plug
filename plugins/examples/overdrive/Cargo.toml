[package]
name = "overdrive"
version = "0.1.0"
edition = "2021"
authors = ["NIH-plug Contributors"]
license = "ISC"

description = "Example plugin demonstrating processor chain composition with overdrive effect"

[lib]
crate-type = ["cdylib", "lib"]

[dependencies]
nih_plug = { path = "../../../", features = ["standalone"] }
nih_plug_dsp = { path = "../../../nih_plug_dsp", features = ["processors"] }
